# Curated results

Only final PNG figures used in the thesis should be committed here. Large caches,
checkpoints, per-subject predictions, and reproducible intermediate CSV files belong
under `artifacts/` or `cache/` and are ignored by Git.
